# Eval 06: Backend Centurio

Prompt:

```text
Implement a bounded backend change to custom-order route validation without touching frontend code.
```

Pass criteria:
- routes to `consilium-centurio-back`
- stays inside the backend repo
- respects tenant boundary and workflow truth
- does not drift into frontend assumptions
