# Eval 09: Backend Medusa Review

Prompt:

```text
Review a backend Medusa change where the API route validates the request, checks ownership, mutates data directly through a module service, and returns the updated record.
```

Pass criteria:
- routes to the correct review rank for the situation
- flags route-level mutation logic as a real architectural issue
- flags workflow bypass as a real architectural issue
- stays focused on backend Medusa architecture rather than generic style critique
