# tribune-reshape - Retrospective

This case introduced Medicus, diagnosis packets, debugging cases, known-gaps doctrine, and Medusa Rig wiring.

The important migration lesson is that debugging state became durable enough to move into case folders, while the runtime skill surfaces still needed explicit path repoints after the docs split.
